$(document).ready(function() {
  var memory = new Game()
  memory.createGame()
  memory.start()
  memory.log()

  // var deck = new Deck()
  // deck.create()
  // deck.shuffle()
  // deck.deal()
  // console.log(deck.deck)
})
